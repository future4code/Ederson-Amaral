~~~javascript
function calculaSalario(carrosVendidos, valorVendas) {
  // Escreva seu código aqui
  const salario = 2000;
  const comissao = 100 + (0,05*(const valorDoCarro));
  const valorDoCarro = [];
  const salarioTotal = ;
  for (let i = 0; i < valorDoCarro; i++) {
    if (valorDoCarro[i] > 0 * comissao) {
      salarioTotal = valorDoCarro + salario
    }
  }
  return valorDoCarro
}
~~~